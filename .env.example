# Скопируй этот файл в `.env` в корне CodeQuest (рядом с package.json).
# Файл `.env` уже в .gitignore — в Git не попадёт.

# Публичный URL твоего AI-сервера (папка `server/` после деплоя), БЕЗ слэша на конце.
# Примеры:
#   Локально:   http://localhost:8787
#   Облако:     https://codequest-api.up.railway.app
#
# Оставь пустым или закомментируй — план и чат работают офлайн (mock / подсказки без модели).
VITE_AI_API_BASE=
