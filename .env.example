# Copy this file to `.env` in the CodeQuest root (next to package.json).
# `.env` is listed in .gitignore — it is never committed.

# Public URL of your AI backend (the `server/` folder after deploy). No trailing slash.
# Examples:
#   Local:  http://localhost:8787
#   Cloud:  https://codequest-api.up.railway.app
#
# Leave empty or comment out — plan and chat use offline mock / rule-based hints (no remote model).
VITE_AI_API_BASE=
